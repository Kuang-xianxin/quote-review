# Optional overrides. Do not commit real credentials.
# The local server automatically creates .data/worker-token when this is absent.
# QUOTE_WORKER_TOKEN=generate-at-least-32-random-characters
# QUOTE_DATA_DIR=.data
# PORT=8765

# Hosted runtime: set WORKER_TOKEN as a secret in Sites, matching the compute worker.
# DEMO_MODE=true
