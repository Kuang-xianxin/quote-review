# 隐私与安全边界

**简体中文** · [English](SECURITY.md)

证据在浏览器或本地 CLI 中处理，应用没有证据上传接口。外部请求用于下载应用和模型文件，托管方可能保留普通访问日志。本地 AI 需要主动启用，不会把证据作为模型 API 请求发送出去。

尽力脱敏后，报告仍可能包含敏感事故信息，请在分享前检查。浏览器历史保存是可选的、仅限本机且可在应用内删除；它未针对共用浏览器配置的其他用户加密。模型缓存由浏览器单独管理。

指令式文本使用有限检测器隔离，不能全面防御提示注入。生成的指令不会被执行，精确引用匹配不代表正确性、权威性或语义支持。切换语言保留证据和引文原样，不应把界面翻译当成脱敏或安全审查。

发现漏洞时，若仓库已启用私密漏洞报告，请使用该功能。不要在公开 issue 发布密钥、私人日志或利用细节。普通功能问题可以使用合成案例公开报告。
