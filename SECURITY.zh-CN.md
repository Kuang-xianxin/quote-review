# 资料与安全边界

公开演示只应使用合成或脱敏资料。原文件上传云端文件存储，文本、模型建议和核对历史保存在云端数据库；运营者的认证计算节点会下载并处理文件。不要把真实供应商合同、个人信息或敏感报价上传到他人的演示站。

随机 HttpOnly、SameSite Cookie 隔离匿名工作区；组、原文件、比价、导出、修改和审计都校验归属。当前没有团队账号和找回链接。清除 Cookie 会失去访问权。会话七天到期；计算节点下次取任务时分批清理过期文件和记录，节点离线时不能保证即时删除。可主动删除比价组及历史。

容量限制：每文件 500 KB、每组 6 份、每会话 5 组、全局 200 文件、16 执行或等待任务、每天 500 新会话。解析限制页数、表格和文本量，有独立进程与超时；POSIX 有内存限制，Windows 暂无硬内存限额。这些是演示保护，不代表已完成安全审计或全面反滥用。

文档文字不可信。模型没有工具，不能执行命令或访问链接；部分可疑指令和原文冲突会提示，但仍可能误提取，必须人工核对。导入拒绝公式单元格，CSV 导出处理公式型文本。

计算节点令牌能领取全部等待文件，务必保密。本地服务默认回环监听。敏感业务应自行部署，补充企业身份、权限生命周期和备份。漏洞请通过仓库的私有安全报告反馈；公开 issue 仅放合成复现。
